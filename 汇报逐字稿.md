# RK3506 星闪多节点采集与轻量化监控系统汇报逐字稿

## 第 1 页：封面

各位评委老师好，我们小组汇报的作品是"基于rk3506的多路智能星闪边缘网关"

这个方案围绕三个关键词展开：RK3506、星闪一对多、低资源 TUI。我们希望解决的是多传感器节点集中采集时，节点数量多、无线接入复杂、上级控制器资源有限的问题。

目前系统已经完成从星闪采集、L2 汇聚、UDP 上传、RK3506 解析，到 TUI 展示的完整链路闭环，并完成了超过 2000 个节点的容量验证。

第三层是 L3 星闪采集节点，直接连接 IMU、电机、电流、电压、温度、电池等传感器，负责采样和基础校验。

第二层是 L2 星闪汇聚控制器，负责接收一个或多个 L3 节点的数据，补充 L2 编号、序号和 CRC 校验信息，然后通过 UDP 上传。

第一层是 RK3506，上面运行 UDP 接收解析服务和 TUI 看板，负责集中解析、状态维护和展示。

接下来展示，L3到L2的星闪传输展示

L2作为星闪服务器先上电，L3作为客户端后上电。

L2定时向3506控制端发送心跳，同时在串口打印调试日志。

L3上电后星闪连接成功，开始向3506上报传感器数据。

接下来展示的是实物验证环境

设备同时连接到手机热点

在3506端启动UDP服务器和节点检测窗口

只上电L2节点，开始发送心跳，3506服务器成功接收。

手动下电L2节点，检测窗口切换为下线状态，上电后在线状态自动恢复。

上电L3节点，检测窗口收到数据，在节点列表添加新的节点

RK3506这类嵌入式控制器作为工业设备不适合长期运行浏览器或重型 GUI，所以我们使用终端TUI完成状态展示。它启动快、占用低，量身定制了一套操作使其更适合现场调试。

深度优化的算法系统可以容纳 2000 个以上节点。服务器在内存中维护节点状态，再定时输出状态快照，TUI 只读取快照展示，所以节点数量增大时仍然可以保持稳定。

现在进行压力测试

先在3506上启动UDP服务器和检测窗口，通过电脑向其快速发送两千个节点。

服务器仍然可以流畅处理，同时具备搜索节点号和删除节点的功能。
