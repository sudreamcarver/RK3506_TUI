# RK3506 星闪多节点采集与轻量化监控系统

（本文不包含学校名称、指导老师等信息）

## 摘要

本作品面向多传感器节点集中采集与状态监控场景，设计并实现了一套基于 RK3506、星闪控制器和 UDP 数据协议的三级节点采集监控系统。系统采用“RK3506 上级控制器 + 第二层星闪汇聚控制器 + 第三层星闪采集节点”的架构：第三层节点连接 IMU、电机、电流、电压、电池、温度等传感器，完成基础采样；第二层星闪控制器作为汇聚层，通过 SLE 星闪链路接收采集节点数据，并封装为统一 UDP 二进制数据帧上传至 RK3506；RK3506 运行 UDP 接收解析服务和轻量化 TUI 看板，完成数据校验、节点状态维护、日志显示和节点详情展示。

本方案突出四个优势：第一，基于 RK3506 与 BearPi Pico H3863 星闪控制器构建，链路具有良好的国产化基础；第二，星闪链路天然适合一对多近端节点接入，可将无线侧节点扩展压力交给第二层汇聚控制器承担；第三，RK3506 端使用 TUI 而非浏览器或重型 GUI，资源占用低，适合嵌入式设备长时间运行；第四，系统采用统一节点命名和状态快照机制，实测可以稳定展示超过 2000 个节点。

系统已完成从 L3 采样、SLE 透传、L2 UDP 封装、RK3506 接收解析到 TUI 展示的完整闭环验证。测试结果表明，系统能够正确解析心跳、节点信息和传感器数据，能够按 `L2/L3/Sensor` 结构维护节点状态，并支持节点搜索、滚动查看和离线状态显示。该方案可扩展到区域化传感器网络、机器人节点监控、工业设备状态采集、教学实验平台和国产化嵌入式监控系统等场景。

【图片占位：系统封面或方案概览图，建议使用 `images/封面.png` 或 `rk3506_sle_udp_presentation_images/slide-01.png`】

## 第一部分 作品概述

### 1.1 功能与特性

本作品实现了一个面向多节点传感器采集的轻量化监控系统。系统主要功能包括：星闪采集节点数据上传、第二层节点数据汇聚、RK3506 UDP 数据接收、二进制协议解析、节点状态维护、TUI 实时显示、节点搜索和节点删除。RK3506 端接收到的数据会按统一格式解析为节点列表、接收日志和节点详情，方便现场调试和运行监控。

系统具有低资源、可扩展、结构清晰的特点。TUI 看板不依赖浏览器或桌面环境，更适合嵌入式设备部署；节点命名采用 `L2-x/L3-y/Sz` 结构，便于快速定位节点来源；星闪链路用于近端一对多采集，UDP 用于汇聚数据上传，降低 RK3506 端直接管理大量无线节点的复杂度。

【图片占位：TUI 节点展示图，建议使用 `images/2000个节点图.png`】

### 1.2 应用领域

该系统适用于需要大量传感器节点接入、集中监控和低资源显示的场景。典型应用包括移动机器人状态采集、工业设备运行监测、分布式电机与电池状态监控、实验室传感器网络、车载或边缘设备节点管理、教学竞赛平台和国产化嵌入式系统验证平台。

在这些场景中，节点数量多、数据类型复杂、部署环境受限是主要问题。本方案通过星闪一对多汇聚降低无线侧复杂度，通过 RK3506 集中解析提升系统可管理性，通过 TUI 展示降低运行资源开销，适合在无完整桌面环境的嵌入式设备上部署。

【图片占位：硬件连接实物图，建议使用 `images/硬件连接图.png`】

### 1.3 主要技术特点

1. 三级节点架构：RK3506 负责集中监控，第二层星闪控制器负责汇聚，第三层星闪控制器负责采集。
2. 星闪一对多扩展：L2 可作为区域汇聚节点，管理多个 L3 采集节点。
3. 统一 UDP 二进制协议：帧头固定 36 字节，包含 `l2_id`、`seq`、`timestamp_us`、`payload_len` 和 `payload_crc32`。
4. 节点统一命名：使用 `L2-1/L3-11/S10007` 形式标识节点，便于检索、更新和删除。
5. 轻量化 TUI：RK3506 端不使用重型 GUI，适合低资源平台长时间运行。
6. 大节点容量：通过内存聚合和状态快照刷新，实测可以展示超过 2000 个节点。

### 1.4 主要性能指标

| 指标 | 当前结果 |
| --- | --- |
| 系统架构 | RK3506 + L2 星闪汇聚控制器 + L3 星闪采集节点 |
| 上行传输方式 | L3 到 L2 使用 SLE，L2 到 RK3506 使用 UDP |
| UDP 监听端口 | 35060 |
| 应用层帧头 | 36 字节固定 FrameHeader |
| 支持消息 | L2_HELLO、L2_HEARTBEAT、NODE_INFO、SENSOR_DATA |
| 离线判断 | L2 心跳超过 3 秒未更新则显示 Offline |
| 节点容量验证 | 2000+ 传感器节点，状态记录 2001 行 |
| TUI 功能 | 日志显示、节点列表、节点详情、搜索、滚动、删除 |

### 1.5 主要创新点

1. 使用星闪作为近端采集链路，结合 RK3506 构建全国产化采集监控方案。
2. 采用 L2 汇聚层，降低 RK3506 直接管理大量无线节点的复杂度。
3. 使用 TUI 代替浏览器界面，显著降低嵌入式端展示资源开销。
4. 建立统一节点命名与协议解析机制，支持大规模节点检索和状态维护。
5. 通过状态快照机制提升大量节点场景下的显示稳定性。

### 1.6 设计流程

系统设计流程如下：首先确定三级节点架构和数据链路方向；随后定义 L2 到 RK3506 的 UDP 二进制协议；接着实现 RK3506 端 UDP 接收解析服务和 TUI 看板；然后编写宿主机测试发送工具，完成不同类型传感器数据模拟；最后进行本地和板端测试，验证心跳、节点更新、离线显示、滚动搜索和 2000+ 节点容量。

【图片占位：系统架构图或 PPT 第 3 页，建议使用 `rk3506_sle_udp_presentation_images/slide-03.png`】

## 第二部分 系统组成及功能说明

### 2.1 系统整体框图

系统整体由三层组成：第一层为 RK3506 上级控制器，负责 UDP 接收、协议解析、状态维护和 TUI 展示；第二层为星闪汇聚控制器，负责接收第三层采集节点数据，并转换为统一 UDP 帧上传；第三层为星闪采集节点，负责连接具体传感器并生成采样数据。

节点命名采用统一规则：

| 字段 | 含义 | 来源 |
| --- | --- | --- |
| `L2-1` | 第二层星闪汇聚控制器编号 | UDP FrameHeader.l2_id |
| `L3-11` | 第三层采集节点编号 | SENSOR_DATA / NODE_INFO payload |
| `S10007` | 传感器编号 | SENSOR_DATA / NODE_INFO payload |
| `L2-1/L3-11/S10007` | 完整节点 key | l2_id + l3_id + sensor_id |

该命名方式保证节点可以被稳定创建、更新、检索和删除。

【图片占位：三级系统框图，建议使用 `rk3506_sle_udp_presentation_images/slide-03.png`】

### 2.2 硬件系统介绍

#### 2.2.1 硬件整体介绍

硬件系统由 RK3506 开发板和 BearPi Pico H3863 星闪开发板组成。RK3506 作为上级控制器，运行 UDP 接收服务和 TUI 看板；星闪开发板作为 L2/L3 控制器，用于验证星闪采集链路和数据上传流程。

当前验证环境中，RK3506 负责集中接收和显示，星闪开发板承担采集和汇聚角色。通过该组合可以验证国产化芯片平台、星闪通信链路和嵌入式监控软件之间的协同能力。

【图片占位：RK3506 开发板图，建议使用 `images/3506开发板图片背面.jpg`】

【图片占位：星闪开发板图，建议使用 `images/星闪开发板正面.jpg`】

#### 2.2.2 机械设计介绍

本作品当前重点为电子系统和通信链路验证，未设计独立机械结构。实物连接采用开发板和线缆搭建方式，便于快速调试 RK3506、星闪开发板和上位机之间的数据链路。后续如果进入产品化阶段，可根据部署场景设计外壳、固定结构、接口防护和散热结构。

【图片占位：整体实物连接图，建议使用 `images/硬件连接图.png`】

#### 2.2.3 电路各模块介绍

电路模块主要包括 RK3506 控制器模块、星闪通信控制器模块、供电与 USB 调试接口、传感器扩展接口等。RK3506 侧负责网络接收和状态显示；星闪控制器侧负责 SLE 通信和数据封装；传感器接口可扩展 IMU、电流、电压、电机、电池等数据源。

系统信号流为：传感器数据进入 L3 星闪采集节点，L3 通过 SLE 上传给 L2，L2 完成校验和 UDP 封装，最后经 UDP 发送到 RK3506。RK3506 不直接解析星闪底层通信，只处理统一的上行数据协议。

【图片占位：RK3506 板卡正反面，建议使用 `images/3506开发板图片正面.jpg` 与 `images/3506开发板图片背面.jpg`】

【图片占位：星闪板卡正反面，建议使用 `images/星闪开发板正面.jpg` 与 `images/星闪开发板背面.jpg`】

### 2.3 软件系统介绍

#### 2.3.1 软件整体介绍

软件系统分为三个部分：RK3506 UDP 接收解析服务、TUI 看板、宿主机测试发送工具。UDP 接收解析服务负责监听 35060 端口，校验 FrameHeader 和 CRC32，解析心跳、节点信息和传感器数据，并维护节点状态；TUI 看板读取状态快照和日志文件，显示 Receive Log、Nodes 和 Node Detail；宿主机测试工具用于模拟 L2 上传数据，支持循环发送和压力测试。

这种设计将协议解析、状态维护和界面展示解耦。接收端可以承受较高频率的数据上报，TUI 只读取定时刷新的状态快照，从而避免大量节点场景下界面频繁刷新造成卡顿。

【图片占位：软件模块图，建议使用 `rk3506_sle_udp_presentation_images/slide-11.png`】

#### 2.3.2 软件各模块介绍

**UDP 接收解析模块**

输入为 UDP datagram，输出为节点状态记录和接收日志。处理流程为：读取 UDP 数据报，校验长度、magic、version、header_size、payload_len 和 payload_crc32；根据 msg_type 分发到 L2_HELLO、L2_HEARTBEAT、NODE_INFO、SENSOR_DATA 等解析流程；解析成功后更新节点状态。

**节点状态模块**

输入为解析后的 L2、L3、Sensor 信息，输出为状态快照文件。节点使用 `L2-x/L3-y/Sz` 作为 key。L2 心跳超过 3 秒未更新时显示 Offline，L3 传感器节点跟随所属 L2 状态显示。节点不会因超时自动删除，便于现场排查历史节点。

**TUI 显示模块**

输入为状态快照和接收日志，输出为终端界面。界面包括 Receive Log、Nodes、Node Detail 三部分，支持滚动、搜索和删除。TUI 的优势是资源占用低，不依赖桌面环境，适合 RK3506 长期运行。

**测试发送模块**

输入为传感器类型、数量和发送间隔，输出为模拟 UDP 上行数据。该模块支持 IMU、MOTOR、BATTERY、CURRENT 等类型，可用于验证协议解析、节点生成、压力测试和 TUI 展示效果。

【图片占位：TUI 界面图，建议使用 `images/2000个节点图.png`】

【图片占位：L2 上传串口日志图，建议使用 `images/数据串口截图.png`】

## 第三部分 完成情况及性能参数

### 3.1 整体介绍

目前系统已经完成 RK3506、星闪开发板、UDP 接收解析服务和 TUI 看板的整体联调。实物链路中，星闪开发板负责模拟 L2/L3 采集与汇聚，RK3506 端运行接收解析服务和轻量化看板。系统能够完成从数据生成、链路上传、协议解析到界面展示的闭环。

【图片占位：整体实物连接图，建议使用 `images/硬件连接图.png`】

### 3.2 工程成果

#### 3.2.1 机械成果

当前阶段未设计独立机械结构，系统以开发板实物连接为主。后续可根据应用环境进行模块化外壳、接口固定、安装结构和防护设计。

【图片占位：整体实物连接照片，建议使用 `images/硬件连接图.png`】

#### 3.2.2 电路成果

电路成果主要体现为 RK3506 开发板和星闪开发板的联调。RK3506 负责运行监控端软件，星闪开发板负责采集与汇聚侧验证。系统已经验证两类硬件平台可以通过 UDP 上行协议完成数据交互。

【图片占位：RK3506 开发板照片，建议使用 `images/3506开发板图片背面.jpg`】

【图片占位：星闪开发板照片，建议使用 `images/星闪开发板正面.jpg`】

#### 3.2.3 软件成果

软件成果包括 UDP 接收解析服务、TUI 监控界面和测试发送工具。TUI 已经能够显示接收日志、节点列表、节点详情、在线离线状态，并支持滚动和搜索。压力测试中，系统成功展示超过 2000 个节点，证明 TUI 与状态维护机制具备较好的扩展能力。

【图片占位：TUI 软件成果图，建议使用 `images/2000个节点图.png`】

### 3.3 特性成果

| 特性 | 实现结果 |
| --- | --- |
| 星闪一对多架构 | 采用 L2 汇聚层，具备扩展多 L3 节点的结构基础 |
| 全国产链路 | RK3506 + BearPi Pico H3863 星闪控制器 |
| UDP 二进制协议 | 36 字节 FrameHeader，支持 CRC32 校验 |
| 节点命名 | `L2-x/L3-y/Sz`，支持稳定检索和更新 |
| 心跳机制 | L2 每秒心跳，3 秒未更新显示 Offline |
| TUI 低资源展示 | 不依赖浏览器或桌面环境 |
| 大规模节点显示 | 实测 2001 行状态记录，超过 2000 个传感器节点 |
| 传感器类型 | 支持 IMU、MOTOR、BATTERY、CURRENT、VOLTAGE、TEMPERATURE 等 |

【图片占位：2000+ 节点验证图，建议使用 `images/2000个节点图.png`】

【图片占位：串口上传与 UDP 帧日志图，建议使用 `images/数据串口截图.png`】

## 第四部分 总结

本作品完成了一套基于 RK3506 和星闪控制器的多节点传感器采集监控系统。系统通过三级架构将采集、汇聚和集中显示分离，既降低了 RK3506 直接管理大量无线节点的复杂度，也发挥了星闪一对多接入的优势。RK3506 端采用 UDP 二进制协议和 TUI 轻量化看板，实现了低资源、可扩展、易调试的嵌入式监控方案。当前系统已完成链路闭环和 2000+ 节点显示验证，具备继续扩展多 L2、多 L3、告警消息和下发控制能力的基础。

### 4.2 心得体会

本项目的核心难点不只是完成数据发送和接收，而是如何让系统在节点数量增大后仍然保持结构清晰、界面稳定和调试方便。最初设计中，如果每收到一个 UDP 包就直接刷新显示或重写完整状态，很容易在压力测试中出现性能瓶颈。后续通过接收端内存聚合、状态文件定时刷新和 TUI 只读快照的方式，系统在大量节点场景下更加稳定。

另一个重要收获是协议设计必须提前考虑扩展性。通过将 `l2_id` 放在 FrameHeader 中，将 `l3_id` 和 `sensor_id` 放在 payload 中，系统可以形成统一的节点 key。这样无论后续接入多少个 L2 或 L3，RK3506 端都可以使用同一套命名规则维护节点状态。`seq`、`payload_len` 和 `payload_crc32` 的加入，也让接收端具备了判断数据完整性和链路异常的能力。

在界面设计方面，TUI 比浏览器界面更适合当前的嵌入式部署目标。它资源占用低，启动速度快，便于在串口、SSH 或本地终端中查看状态。通过 Receive Log、Nodes 和 Node Detail 三个区域，系统既能观察数据是否到达，也能快速定位某个节点的详细数据。节点搜索和滚动功能在 2000+ 节点测试中非常关键，否则大量节点会难以人工检索。

在硬件链路方面，RK3506 与星闪控制器的组合验证了国产化方案的可行性。星闪用于近端传感器节点接入，RK3506 用于上级控制和集中展示，二者职责清晰。后续如果继续完善，可以重点扩展 L2 对多 L3 的真实星闪组网、L2 离线缓存、RK3506 下发控制命令、告警事件显示和数据持久化记录，使系统从验证原型进一步走向可部署应用。

## 第五部分 参考文献

[1] LibreOffice Writer. 报告模板说明文档.

[2] BearPi. BearPi Pico H3863 SLE 网关透传测试文档.

[3] Rockchip. RK3506 相关开发板与嵌入式 Linux 开发资料.

[4] IEEE. IEEE 802.3 CRC-32 相关标准说明.

[5] FTXUI Project. FTXUI 终端用户界面库文档.

[6] Linux Manual Pages. socket、recvfrom、sendto、UDP 通信相关接口说明.

[7] CMake Project. CMake 构建系统官方文档.

[8] C++ Reference. C++17 标准库容器、字符串和文件 I/O 参考资料.
